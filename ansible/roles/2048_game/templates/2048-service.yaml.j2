apiVersion: v1
kind: Service
metadata:
  name: "service-2048"
  namespace: "{{game_namespace}}"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "2048"
